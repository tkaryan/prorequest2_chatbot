# ============================================================
#  requirements.txt — ProRequest WhatsApp Chatbot
#  Flask + MySQL + Algolia + Gemini AI + WhatsApp API
# ============================================================

# ── Web framework ────────────────────────────────────────────
Flask==3.1.1
Werkzeug==3.1.3          # WSGI utils (requerido por Flask)
gunicorn==23.0.0          # Servidor WSGI para producción (en vez de flask run)

# ── HTTP & Networking ────────────────────────────────────────
requests==2.32.4
urllib3==2.4.0            # Requerido por requests
certifi==2025.1.31        # Certificados SSL actualizados

# ── Base de datos: MySQL ──────────────────────────────────────
mysql-connector-python==9.3.0
PyMySQL==1.1.1            # Driver alternativo/complementario (más liviano)
SQLAlchemy==2.0.41        # ORM opcional pero muy útil para queries complejas

# ── Búsqueda: Algolia ─────────────────────────────────────────
algoliasearch==3.0.0

# ── Variables de entorno ──────────────────────────────────────
python-dotenv==1.1.1

# ── Seguridad & Validación ────────────────────────────────────
cryptography==44.0.3      # Para tokens seguros y cifrado
itsdangerous==2.2.0       # Firmado seguro de datos (usado por Flask internamente)

# ── Utilidades generales ──────────────────────────────────────
python-dateutil==2.9.0    # Manejo de fechas robusto
pytz==2025.1              # Zonas horarias (para timestamps de Lima/Perú)
retry==0.9.2              # Decorador @retry para llamadas a APIs externas
tenacity==9.1.2           # Alternativa moderna a retry, más configurable

# ── Logging & Monitoreo ───────────────────────────────────────
colorlog==6.9.0           # Logs coloreados en consola (mucho más legible en dev)

# ── Testing (opcional pero recomendado) ───────────────────────
pytest==8.3.5
pytest-flask==1.3.0       # Fixtures para testear endpoints Flask

# ============================================================
# NOTAS DE VERSIONES
# ============================================================
# Python recomendado : 3.10 – 3.12
# algoliasearch 3.x  : API síncrona — si migras a v4 el cliente cambia bastante
# gunicorn           : no funciona en Windows; usar waitress en ese caso
# ============================================================